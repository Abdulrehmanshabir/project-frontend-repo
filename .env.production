# Shared env for all modes (dev and prod)
# Keep only values that are identical across environments here.
VITE_AUTH_LOGIN_PATH=/auth/login
VITE_AUTH_REGISTER_PATH=/auth/register
VITE_API_URL=http://localhost:3000
